# public 目录指南

## 资源管理
- 存放不会经过打包处理的静态资源（图标、manifest 等），文件名保持小写短横线风格。
- 更新 `manifest.webmanifest` 时同步检查应用名称、主题色与图标路径是否正确。

## 最佳实践
- 尽量使用矢量图（SVG）或体积小的位图，避免将大于 200KB 的资源放入仓库。
- 不在该目录存放敏感信息或动态配置，环境变量应通过 `.env` 管理。

## 缓存与版本
- 若替换现有资源，请考虑文件名 hash 或修改时间戳，以避免浏览器缓存导致的更新失效。
